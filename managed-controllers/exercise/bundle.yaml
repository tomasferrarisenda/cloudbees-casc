apiVersion: "1"
id: "exercise"
description: "A simple bundle that sets up a master with the git-client plugin"
version: "10"
parent: "shared"
jcasc:
  - "jenkins.yaml"
plugins:
  - "plugins.yaml"
items:
  - "items.yaml"

  